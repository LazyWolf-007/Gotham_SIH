{
  "name": "gotham-sih-root",
  "version": "1.0.0",
  "private": true,
  "description": "Gotham_SIH — Operation Grey Ledger Investigation Workbench",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "build": "npm --prefix app run build",
    "server": "python server.py",
    "check": "python -X utf8 scripts/demo_check.py"
  }
}
